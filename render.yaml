services:
  - type: web
    name: natal-chart-api
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: natal-chart-db
          property: connectionString

databases:
  - name: natal-chart-db
    plan: free
